apiVersion: v1
kind: Service
metadata:
  name: {{ include "edge-site.fullname" . }}
  labels:
    {{- include "edge-site.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "edge-site.selectorLabels" . | nindent 4 }}
